{
    // Configuración de conexión a la base de datos para SQLTools
    "sqltools.connections": [// Puedes agregar múltiples conexiones aquí
        {
            "ssh": "Disabled",
            "previewLimit": 50,
            "server": "localhost",
            "port": 5432,
            "driver": "PostgreSQL",
            "name": "barberia_catracha",
            "database": "barberia_catracha",
            "username": "postgres"
        }
    ],
    "emeraldwalk.runonsave": {// Configuración para ejecutar comandos al guardar archivos
        "commands": [
            {
                "match": "\\.php$",
                "cmd": "C:\\xampp\\php\\php.exe \"${fileDirname}/generar_diagrama.php\""
            }
        ]
    }
}
